pre-commit:
    commands:
        check:
            run: "npx biome check --organize-imports-enabled=true --write --files-ignore-unknown=true --no-errors-on-unmatched {staged_files}"
            stage_fixed: true